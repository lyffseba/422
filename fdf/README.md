# fdf (Mojo)

Wireframe height-map viewer (ASCII projection). Same idea as 42 FdF
without MiniLibX — focus on parsing maps + projection math.

```bash
pixi run mojo run fdf/main.mojo fdf/resources/maps/42.fdf
```
