# corewar (Mojo)

Educational virtual machine + tiny assembler. Same *role* as 42 Corewar
(program representation, fetch/decode/execute), simplified instruction set.

```bash
pixi run mojo run corewar/main.mojo
```
