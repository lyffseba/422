# ft_printf (Mojo)

Variadic-style formatted printing using an explicit `FormatArgs` bag
(Mojo has no C varargs; this preserves the learning goal: parse a format
string and emit conversions).

```bash
pixi run mojo run ft_printf/main.mojo
```
