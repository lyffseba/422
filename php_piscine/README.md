# php_piscine → see `python_piscine/`

421 used PHP as the secondary language track. In 422 that role is
**Python interop**, implemented under [`../python_piscine`](../python_piscine).

This directory exists so the top-level tree matches 421’s shape.
